{
  "name": "root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "clean": "lerna clean",
    "watch": "lerna run --parallel watch",
    "publish": "lerna publish",
    "version": "pnpm lerna version"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.2.1"
  }
}
